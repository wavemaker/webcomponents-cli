export class AppModule implements DoBootstrap {

    constructor(private injector: Injector) {}

    ngDoBootstrap() {
        console.log("--------bootstraping webcomponent---------")
        const mainComponent = createCustomElement(MainComponent, { injector: this.injector });
        customElements.define('wm-prefab-{{prefabName}}', mainComponent);
    }

}
